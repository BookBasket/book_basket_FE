import Library from './Library.svelte';
import Login from './Login.svelte';

// var app = new Library({
// 	target: document.body
// });

var app = new Login({
	target: document.body
})

export default app;
